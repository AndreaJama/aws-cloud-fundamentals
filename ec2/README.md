# EC2 Lab — Conexão e Nginx

Resumo:
- Criar instância EC2 (Ubuntu)
- Ajustar permissões da chave (.pem) no Windows
- Conectar via SSH
- Instalar Nginx

Arquivos:
- connect.ps1 — ajuste de permissões e SSH (Windows) e instalação de nginx (Ubuntu)
